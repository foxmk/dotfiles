[color]
	ui = always

[alias]
	ci = commit -a
	am = commit -a --amend
	st = status -s
	ls = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
	co = checkout

[push]
	default = matching

[core]
	excludesfile = /Users/foxmk/.gitignore_global

[include]
        path = ~/.gitconfig_local
